{
  "name": "rsvp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "sls deploy --stage yan --aws-profile default",
    "remove": "sls remove --stage yan --aws-profile default",
    "deployProd": "sls deploy --aws-profile default",
    "removeProd": "sls remove --aws-profile default"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-pseudo-parameters": "^2.5.0"
  }
}
